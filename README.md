# B2SC

0. Update hyperparameters by taking a look at configure.py
1. Run ```python train.py```.
2. Run ```python trainBulk.py``` to train scVAE.
3. Run ```python B2SC.py``` to train bulkEncoder.
4. Run ```python cluster_celltypes.py``` to plot clusters.